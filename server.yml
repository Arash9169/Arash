name: VPS

on: workflow_dispatch

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Start Server
        run: |
          sudo apt update
          sudo apt install -y openssh-server
          sudo service ssh start
          echo "Server running"
          sleep 21600
